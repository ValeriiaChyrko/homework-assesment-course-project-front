import { Typography } from "@mui/material";
import { styled } from "@mui/system";

export const TaskInformationText = styled(Typography)({
    fontFamily: 'Lora, serif',
    fontWeight: 700,
    fontSize: 22,
    color: '#000000',
});

export const TaskDescriptionText = styled(Typography)({
    fontFamily: 'Lora, serif',
    fontWeight: 500,
    fontSize: 18,
    color: '#000000',
});

export const SectionTitle = styled(Typography)({
    fontFamily: 'Lora, serif',
    fontWeight: 400,
    fontSize: 16,
    color: '#000000',
});

export const AttemptTitle = styled(Typography)({
    fontFamily: 'Lora, serif',
    fontWeight: 600,
    fontSize: 18,
    color: '#000000'
});