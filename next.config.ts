const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'uattpugz9u.ufs.sh',
            },
            {
                protocol: 'https',
                hostname: 'utfs.io',
            },
        ],
    },
    transpilePackages: ["@uploadthing/mime-types"],
};

export default nextConfig;